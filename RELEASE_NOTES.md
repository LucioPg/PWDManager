---

PWDManager v0.2.10

  feat: Simplified Registration — account created automatically on first launch
  feat: Windows Hello is now the only authentication method
  feat: Military-grade (GOD level) master password auto-generated and stored securely
  feat: Username auto-detected from Windows account
  feat: Settings still allows password change (triggers migration)

---

PWDManager v0.2.9.1

  feat: Windows Hello auto-login — automatic login via biometric authentication
  feat: auto-login toggle in general settings and registration form
  feat: BreakingChangeDialog — multi-vault password export dialog before breaking updates
  feat: is_breaking field in update manifest to flag updates requiring data migration
  feat: multi-vault export pipeline for user backup
