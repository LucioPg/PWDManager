---

PWDManager v0.3.1

  fix: Windows Hello prompt now appears correctly after logout
  fix: Auto-login works for all users, including those who registered before the feature existed
  change: Removed auto-login toggle from Settings — now always active when Windows Hello is set up
  fix: Pre-update export dialog now shown for every update, not only breaking changes

---

PWDManager v0.3.0

  feat: Simplified Registration — account created automatically on first launch
  feat: Windows Hello is now the only authentication method
  feat: Military-grade (GOD level) master password auto-generated and stored securely
  feat: Username auto-detected from Windows account
  feat: Settings still allows password change (triggers migration)

---
